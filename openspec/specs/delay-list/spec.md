## Requirements
### Requirement: Delay List table supports delete action
The system SHALL provide a context menu delete action for Delay List rows.

#### Scenario: Delete selected rows
- **WHEN** a user right-clicks selected rows in the Delay List table and chooses delete
- **THEN** the selected rows are removed from the view and data source
